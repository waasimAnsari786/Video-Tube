// constant var for DB name
const DB_NAME = "youTube";
// max file size
const MAX_FILE_SIZE = 100 * 1024 * 1024;
// cookie options
const COOKIE_OPTIONS = {
  httpOnly: true,
  secure: true,
};

export { DB_NAME, MAX_FILE_SIZE, COOKIE_OPTIONS };
